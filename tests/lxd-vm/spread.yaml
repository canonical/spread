project: spread

backends:
    lxd-vm:
        systems:
          # list systems which are known to work as VMs, both 16.04 and 18.04 do
          # not work at all
          - ubuntu-20.04
          - ubuntu-22.04
          - ubuntu-24.04

path: /home/test

suites:
    checks/:
        summary: Verification tasks.


# vim:ts=4:sw=4:et
